export enum RoutesPath  {
  auth = 'auth',
  errors = 'errors',
}

export enum AuthRoutesPath {
  register = 'register',
  login = 'login',
}

export enum ErrorsRouterPath {
  error_403 = 'error-403',
  error_404 = 'error-404'
}
