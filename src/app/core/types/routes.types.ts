export enum RoutesPath  {
  auth = 'auth',
  errors = 'errors',
  main = 'main'
}

export enum AuthRoutesPath {
  register = 'register',
  login = 'login',
}

export enum ErrorsRouterPath {
  error_403 = 'error-403',
  error_404 = 'error-404'
}
